import SectionsSlide from "../../components/Video/SectionSlider";

const Sections: React.FunctionComponent = (props) => {
  return (
    <div>
      <SectionsSlide title="Documentário" videos={[]} />
    </div>
  );
};

export default Sections;
